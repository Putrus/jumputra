#include "../inc/Sequence.hpp"

namespace jp::algorithm
{
   Sequence::Sequence(const std::shared_ptr<logic::Engine>& engine) : Algorithm(engine) {}

   void Sequence::update(float dt)
   {
      
   }
}